# Running ElliottBot locally

This container image does not include the .NET SDK. To build or run ElliottBot locally, install the appropriate .NET SDK (the solution targets .NET used by the project) and then run `dotnet build` from the repository root.
